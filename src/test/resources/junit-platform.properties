# Change the default lifecycle of JUnit so that a new instance of the test class is NOT instantiated for each test method.
# This is good practice anyway, but even more so when using Kotlin.
junit.jupiter.testinstance.lifecycle.default = per_class
